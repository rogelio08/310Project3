#ifndef _util_h
#define _util_h 1

#include <stdio.h>

/*there's not a lot in this file besides the nextCommand function header */
void nextCommandFind(char str[30], int* source, int* destination, int* flag);
void nextCommandWrite(char str[30], int* source, int* destination);
#endif
